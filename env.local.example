# === Supabase 必需 ===
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 仅服务端使用（可选，用于绕过RLS等服务端操作）
SUPABASE_SERVICE_ROLE_KEY=

# === 应用地址（用于回调、OpenRouter Header 等） ===
# 本地开发端口为 8080（与 package.json 的 dev 脚本一致）
NEXT_PUBLIC_APP_URL=http://localhost:8080
NEXT_PUBLIC_SITE_URL=http://localhost:8080

# === OpenRouter（改写功能）===
OPENROUTER_API_KEY=

# === 大家啦 / 灵感相关 ===
# 公网 API 基础地址（保留默认即可）
NEXT_PUBLIC_INSPIRATION_API_URL=https://inspiration-hub-api-k5oeg3wd9-artiver-sites-projects.vercel.app
# 前端使用的公开 Key（可选，建议配置你自己的）
NEXT_PUBLIC_INSPIRATION_API_KEY=

# === 微信抓取相关（可选）===
DAJIALA_API_KEY=
JIZHILE_API_KEY=
JIZHILE_VERIFY_CODE=

# === 热榜数据（可选）===
# 如不配置将使用代码内默认地址
TOPHUB_BASE_URL=https://www.tophubdata.com/api
TOPHUB_API_KEY=

# === Node/启动（可选）===
NODE_ENV=development
# 仅生产 start 时可能读取此端口；开发已通过脚本 -p 8080 指定
# PORT=8080
